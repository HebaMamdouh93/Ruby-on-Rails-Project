<%= form_for([@lecture, @lecture.comments.build]) do |f| %>
    
    <p>
 
      <%= f.text_area :content %>
    </p>
    <p>
      <%= f.submit "Create Comment", :id => "save-button" %>
    </p>

<% end %>    